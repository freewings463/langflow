"""
模块名称：自定义代码解析器导出门面

模块目的：统一导出 `CodeParser`，供其他模块复用解析能力。
使用场景：在组件加载/校验阶段直接引用解析器。
主要功能包括：
- 暴露 `CodeParser` 类作为单一入口

关键组件：
- `CodeParser`：源码解析与结构化抽取器

设计背景：该目录仅包含单一解析器，直接导出可简化导入路径。
注意：若后续扩展多个解析器，可切换为延迟导入以降低依赖加载成本。
"""

from .code_parser import CodeParser

__all__ = ["CodeParser"]
