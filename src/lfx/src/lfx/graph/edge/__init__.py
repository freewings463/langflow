"""
模块名称：lfx.graph.edge

本模块提供图边相关的包级聚合入口，主要用于统一导入 `edge` 子模块。主要功能包括：
- 功能1：作为 `graph.edge` 包的命名空间入口

关键组件：
- `base`：运行时边模型
- `schema`：边协议与类型模型
- `utils`：边相关的工具函数集合

设计背景：保持图结构子模块组织一致，降低引用路径复杂度。
注意事项：本文件不包含业务逻辑，仅负责包初始化。
"""
