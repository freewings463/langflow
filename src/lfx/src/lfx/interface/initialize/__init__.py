"""
模块名称：接口初始化子包

本模块聚合初始化相关逻辑的子模块。
主要功能包括：
- 导出组件加载与构建流程

设计背景：将初始化逻辑集中管理，便于维护。
"""

# 注意：统一导出初始化子模块。
"""
模块名称：初始化子包入口

本模块提供初始化相关功能的统一导出。主要功能包括：
- 暴露 `loading` 子模块

关键组件：
- `loading`：组件实例化与构建入口

设计背景：将初始化逻辑集中管理，便于模块化与测试。
使用场景：组件实例化、参数加载与构建流程。
注意事项：导入 `loading` 可能触发依赖加载。
"""

from . import loading

__all__ = ["loading"]
