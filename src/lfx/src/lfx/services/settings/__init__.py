"""
模块名称：settings

本模块作为设置服务的包入口，集中导出工厂与服务实现。
主要功能包括：
- 统一暴露设置服务构建入口
- 规范外部导入路径

关键组件：
- factory：设置服务工厂
- service：设置服务定义

设计背景：保持包内结构清晰，避免外部直接依赖内部文件路径。
注意事项：仅暴露必要对象，新增实现需同步更新导出列表。
"""

from . import factory, service

__all__ = ["factory", "service"]
