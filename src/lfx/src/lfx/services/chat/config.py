"""
模块名称：lfx.services.chat.config

本模块提供聊天服务配置项，主要用于集中声明默认行为开关。主要功能包括：
- 功能1：定义聊天服务的默认流式开关

关键组件：
- `ChatConfig`：配置容器

设计背景：避免硬编码分散在业务逻辑中，便于集中调整。
注意事项：仅定义默认值，不做运行期校验。
"""


class ChatConfig:
    """聊天服务配置容器。

    契约：`streaming` 控制默认是否开启流式输出。
    关键路径：无运行逻辑，仅提供默认值。
    """

    streaming: bool = True
