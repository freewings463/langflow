"""
模块名称：lfx.services.chat

本模块提供聊天服务相关的配置与协议模型，主要用于统一缓存接口约定与默认配置。主要功能包括：
- 功能1：导出聊天服务配置（`ChatConfig`）
- 功能2：定义缓存协议接口（`GetCache`/`SetCache`）

关键组件：
- `config`：聊天服务配置
- `schema`：缓存协议定义

设计背景：隔离服务配置与协议约定，避免在业务层散落定义。
注意事项：协议仅定义签名，不包含具体实现。
"""
