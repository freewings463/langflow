"""模块名称：文本切分组件包

本模块作为 textsplitters 包的入口，当前仅提供包级说明与导出占位。
主要功能包括：描述该包用途并维护导出列表。

关键组件：
- `__all__`：包级导出列表（当前为空）

设计背景：预留文本切分组件的统一入口，便于后续扩展与集中导出。
注意事项：新增组件时需要同步更新 `__all__`。
"""

__all__: list[str] = []
