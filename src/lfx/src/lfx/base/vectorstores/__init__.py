"""
模块名称：向量库组件包入口

本模块用于聚合向量库组件与辅助工具的包级入口，供上层模块统一导入。主要功能包括：
- 作为 `lfx.base.vectorstores` 的包级入口
- 统一暴露向量库基类与相关工具模块

关键组件：
- 见同目录 `model.py` / `utils.py` / `vector_store_connection_decorator.py`

设计背景：向量库组件被多个子系统复用，需要稳定的导入路径。
使用场景：上层检索组件与连接装饰器通过本包入口访问。
注意事项：本包仅提供入口，不包含运行时逻辑。
"""
