"""
模块名称：document_transformers 包入口

本模块提供文档转换器子包的入口说明，主要用于明确该子包的职责与稳定导入路径。
主要功能包括：
- 说明该子包负责文档转换器组件的抽象层
- 指向实现所在的 `model` 模块以便定位代码

关键组件：
- `LCDocumentTransformerComponent`：位于 `model`

设计背景：保持 LFX 基础组件目录结构清晰，便于迁移与依赖管理。
注意事项：本文件仅提供说明，不直接导出符号，使用时请从 `model` 导入。
"""
