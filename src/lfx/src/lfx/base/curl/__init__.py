"""
模块名称：curl 解析工具包

本模块提供对 `curl` 命令的解析与上下文建模能力，主要用于将命令行输入转换为请求构造参数。主要功能包括：
- 将 `curl` 文本解析为结构化参数（见 `parse_curl_command`）
- 生成请求上下文（方法/URL/头/代理/认证）

关键组件：
- `parse.py`：核心解析逻辑与数据契约

设计背景：为兼容上游 `uncurl` 且满足 Langflow 的解析边界需求，本包保留现有语义并补齐本地缺口。
注意事项：该包仅进行字符串解析，不发起网络请求。
"""
