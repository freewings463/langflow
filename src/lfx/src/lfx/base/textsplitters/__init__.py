"""
模块名称：`textsplitters` 包初始化

本模块用于标识 `lfx.base.textsplitters` 的包边界，集中放置文本分割器基础定义。
主要功能包括：
- 作为文本分割器基类与公共导出的稳定入口

关键组件：
- 无（仅用于包标识与分层说明）

设计背景：将文本分割器基础能力下沉到统一包路径，便于上层一致依赖。
注意事项：本包仅承载基础定义/公共导出，不直接实现具体分割策略。
"""
