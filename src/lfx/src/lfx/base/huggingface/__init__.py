"""
模块名称：`huggingface` 包初始化

本模块用于标识 `lfx.base.huggingface` 的包边界，集中放置 Hugging Face 适配层。
主要功能包括：
- 作为适配层模块的稳定入口，供上层按路径引用

关键组件：
- 无（仅用于包标识与分层说明）

设计背景：将第三方适配逻辑与核心模型基类分层隔离，降低耦合。
注意事项：本包仅承载适配与桥接相关实现，不应直接放置业务逻辑。
"""
