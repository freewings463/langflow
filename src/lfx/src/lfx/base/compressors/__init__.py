"""
模块名称：压缩器组件包入口

本模块用于聚合压缩器相关的基础组件定义，供上层组件按需导入。主要功能包括：
- 作为 `lfx.base.compressors` 的包级入口
- 统一暴露压缩器组件与模型定义

关键组件：
- 见同目录 `model.py`

设计背景：压缩器组件在多个子系统复用，需要稳定的包入口以避免循环导入。
使用场景：上层模块通过统一入口导入压缩器基类与组件定义。
注意事项：本包目前仅提供入口，不包含运行时逻辑。
"""
