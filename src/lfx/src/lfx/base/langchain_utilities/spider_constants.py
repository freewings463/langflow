"""
模块名称：爬取模式常量

本模块提供 LangChain Spider 相关的模式枚举，主要用于约束配置项的可选值。主要功能包括：
- 统一 `scrape` 与 `crawl` 两类模式名称

关键组件：
- `MODES`：允许的模式列表

设计背景：避免在组件配置中出现拼写不一致导致的运行期错误。
注意事项：新增模式需同步更新依赖该列表的校验逻辑与前端选项。
"""

MODES = ["scrape", "crawl"]
