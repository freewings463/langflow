"""
模块名称：memory 子包入口

本模块提供基础记忆组件的入口说明，主要用于明确 LFX 记忆组件的抽象职责与导入路径。
主要功能包括：
- 说明记忆组件负责会话消息的读取与写入契约
- 指向实现所在的 `memory` 与 `model` 模块以便定位代码

关键组件：
- `BaseMemoryComponent`：位于 `memory`
- `LCChatMemoryComponent`：位于 `model`

设计背景：统一记忆组件抽象，便于不同存储后端实现与迁移。
注意事项：本文件仅提供说明，不直接导出符号，使用时请从对应模块导入。
"""
