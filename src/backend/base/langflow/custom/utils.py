"""
模块名称：自定义组件工具函数兼容导出

本模块转发 `lfx.custom.utils` 工具函数，主要用于旧路径兼容。主要功能包括：
- 复用新版自定义组件工具函数
- 保持历史导入路径可用

关键组件：
- utils 中的导出函数（按 lfx 定义）

设计背景：迁移到 `lfx` 后需兼容旧路径。
注意事项：此处使用 `import *`，导出范围由上游模块控制。
"""

from lfx.custom.utils import *  # noqa: F403
